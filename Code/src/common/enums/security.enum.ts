export enum AudienceEnum {
    USER = "user",
    SYSTEM = "system",
}

export enum TokenTypeEnum {
    ACCESS = "access",
    REFRESH = "refresh",
}

export enum LogoutEnum {
    ALL = "all",
    ONE = "one",
}